# 本地陪伴型 Agent · 架构宪章 v1.0（历史基线 · 只读）

> **状态**：**历史基线**，仅供追溯；**冲突时以** [`ARCHITECTURE_v2.0_zh.md`](./ARCHITECTURE_v2.0_zh.md) **为准**。  
> **日期**：v1.0 冻结正文；2026-06-12 起由 v2.0 承接演进。

---

## 一、核心原则（不可妥协）

1. **陪伴优先**：交互质量与一致性高于工具能力；人设边界稳定，任何输出必须经过人格锁。
2. **工具为次**：工具链是可选插件，不得污染主路径的信任与延迟。
3. **记忆透明**：用户可看见、编辑、删除、导出所有记忆，并理解「为何被召回」。
4. **隐私第一**：默认本地闭环，网络出站须显式授权；禁止静默上云。
5. **出站可审计**：任何云端请求必须经过策略许可，生成 **Consent Artifact**（最小上传、目的、范围、二次确认）。
6. **硬项同权**：危机分级/固定话术、评测回归集、数据导出/迁移/密钥说明与功能同权重。

---

## 二、系统分层（三层七域，九模块）

### 分层与网络边界

| 层 | 域（模块） | 公网/出站 |
|----|----------------|-----------|
| **A 层 · 策略壳** | A1 UI 宿主、A2 策略引擎、**A3 出站管理器** | **仅 A3 可出站**，且须经 A2 显式许可 |
| **B 层 · 陪伴核** | B1 人格与会话、B2 记忆生命周期、B3 安全边界分级、B4 本地润色器 | **禁止** |
| **C 层 · 算力与 IO** | C1 本地推理后端、C2 存储与索引 | **禁止** |

### 依赖方向（调用方向；禁止反向）

- `A1 → A2`
- `A2 → B`
- `A2 → A3`
- `B → C`
- **禁止** `B → A`、`C → A`、`C → B`。
- **B / C 禁止 `import` 网络库**；唯一跨网边界为 **A3**。

### 物理包映射（Python）

- `shell/` → A 层
- `core/` → B 层
- `runtime/` → C 层
- `shared/` → 跨层 DTO（**禁止反向依赖**）

---

## 三、关键设计锚点

### 记忆

- **SQLite** 权威源；**FTS** + **时间衰减** + **`matched_on`**。
- 写入触发器注册表；默认仅 **显式保存**。
- 预留 **embedding** 字段。

### 混合推理

- 云端仅 **用户显式触发**；结果经 **B4 润色**；失败 **硬降级**本地安全回复。

### 安全

- 规则分级 + **版本化固定话术**；高危词 **无条件阻断**。

### 数据可携带

- 单文件导出包；明文警告 + 可选密码加密；`integrity` 占位。

---

## 四、MVP 默认值

| 项 | 默认 |
|----|------|
| 模式 | `local_only` |
| 记忆 | **默认关**；写入仅显式保存 |
| 云端 | **仅强推理**；无联网检索 |
| 自动路由上云 | **永不进主线** |

---

## 五、已解决开放项（v1.0 冻结）

- 人设 YAML 最小字段集：`id`、`system_prompt`、`tone_keywords`、`forbidden`、`safety`、`persona_version`。
- 记忆编辑 **下一轮起生效**。
- 导出 **`integrity` 占位** + payload digest。

---

**版本**：v1.0 · **性质**：历史基线（不随 Sprint 更新）
